def validMountainArray(arr):
    ''' 
    - Double check if the array is monotonically 
    increasing and then decreasing
      - If so, return True (and vice versa)

    -------------------------------------------------

    Q: How do we know at what point the array will 
    monotonically decrease? 

    ** TODO: Try this problem later 
    ''' 
    
    is_increase
    is_decrease 


    if len(arr) < 3:
      return False 


    for index in range(len(arr))
